/** Browser preview values: loud enough for immediate playback, bounded by a compressor in PlayerContext. */
export const DEFAULT_PREVIEW_VOLUME = 1;
export const PREVIEW_MASTER_GAIN = 0.62;
export const PREVIEW_OUTPUT_FLOOR = 0.035;
export const PREVIEW_VOICE_GAIN = 0.2;
